// @ts-nocheck
/* eslint-disable */
/* prettier-ignore */
// This file is automatically generated. Any manual editions will be replaced.

import { adapter } from "./adapter.js";
import { templateToParams } from "kineo/runtime";

export { adapter as $adapter };

// TODO transactions

export async function $exec(
  strings: TemplateStringsArray,
  ...values: unknown[]
) {
  const result = await (await adapter).exec(templateToParams(strings, values));
  return {
    rows: (result.rows as any[]) ?? [],
    rowCount: result.rowCount ?? 0,
  };
}

export async function $close() {
  return await (await adapter).close();
}
