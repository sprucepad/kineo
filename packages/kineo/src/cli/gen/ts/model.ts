/* prettier-ignore */
/* eslint-disable */
// This file is automatically generated. Any manual editions will be replaced.

// @ts-expect-error {{REMOVE_ON_GENERATE}}
import { adapter } from "../adapter.js";
import { createOperation, type ModelTypes } from "kineo/runtime";
import {
  parseAggregateStatement,
  parseCountStatement,
  parseDeleteStatement,
  parseFindStatement,
  parseGroupByStatement,
  parseInsertStatement,
  parseUpdateStatement,
  parseUpsertStatement,
} from "kineo/ir";
import type { ParsedModel } from "kineo/schema";

`{{extraImports}}`;

// @ts-expect-error {{REMOVE_ON_GENERATE}}
export const $shape: ParsedModel = `{{shape}}`;
export const $adapter = adapter;

export type I = `{{i}}`;
export type IO = `{{io}}`;
export type R = `{{r}}`;
export type RO = `{{ro}}`;

export const find: <O extends ModelTypes.FindOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.FindReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseFindStatement,
  "row",
);

export const findMany: <O extends ModelTypes.FindOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.FindReturn<I, IO, R, RO, O, true>> = createOperation(
  $adapter,
  $shape,
  parseFindStatement,
  "rows",
);

export const create: <O extends ModelTypes.CreateOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.CreateReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseInsertStatement,
  "row",
);

export const createMany: <O extends ModelTypes.CreateOpts<I, IO, R, RO, true>>(
  opts?: O,
) => Promise<ModelTypes.CreateReturn<I, IO, R, RO, O, true>> = createOperation(
  $adapter,
  $shape,
  parseInsertStatement,
  "rows",
);

export const createReturn: <
  O extends ModelTypes.CreateReturnOpts<I, IO, R, RO>,
>(
  opts?: O,
) => Promise<ModelTypes.CreateReturnReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseInsertStatement,
  "row",
);

export const createManyReturn: <
  O extends ModelTypes.CreateReturnOpts<I, IO, R, RO>,
>(
  opts?: O,
) => Promise<ModelTypes.CreateReturnReturn<I, IO, R, RO, O, true>> =
  createOperation($adapter, $shape, parseInsertStatement, "rows");

export const update: <O extends ModelTypes.UpdateOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.UpdateReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseUpdateStatement,
  "row",
);

export const updateMany: <O extends ModelTypes.UpdateOpts<I, IO, R, RO, true>>(
  opts?: O,
) => Promise<ModelTypes.UpdateReturn<I, IO, R, RO, O, true>> = createOperation(
  $adapter,
  $shape,
  parseUpdateStatement,
  "rows",
);

export const updateReturn: <
  O extends ModelTypes.UpdateReturnOpts<I, IO, R, RO>,
>(
  opts?: O,
) => Promise<ModelTypes.UpdateReturnReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseUpdateStatement,
  "row",
);

export const updateManyReturn: <
  O extends ModelTypes.UpdateReturnOpts<I, IO, R, RO>,
>(
  opts?: O,
) => Promise<ModelTypes.UpdateReturnReturn<I, IO, R, RO, O, true>> =
  createOperation($adapter, $shape, parseUpdateStatement, "rows");

export const upsert: <O extends ModelTypes.UpsertOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.UpsertReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseUpsertStatement,
  "row",
);

export const upsertMany: <O extends ModelTypes.UpsertOpts<I, IO, R, RO, true>>(
  opts?: O,
) => Promise<ModelTypes.UpsertReturn<I, IO, R, RO, O, true>> = createOperation(
  $adapter,
  $shape,
  parseUpsertStatement,
  "rows",
);

const _delete: <O extends ModelTypes.DeleteOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.DeleteReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseDeleteStatement,
  "row",
);
export { _delete as delete };

export const deleteMany: <O extends ModelTypes.DeleteOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.DeleteReturn<I, IO, R, RO, O, true>> = createOperation(
  $adapter,
  $shape,
  parseDeleteStatement,
  "rows",
);

export const count: <O extends ModelTypes.CountOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.CountReturn> = createOperation(
  $adapter,
  $shape,
  parseCountStatement,
  "rowCount",
);

export const aggregate: <O extends ModelTypes.AggregateOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.AggregateReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseAggregateStatement,
  "row",
);

export const groupBy: <O extends ModelTypes.GroupByOpts<I, IO, R, RO>>(
  opts?: O,
) => Promise<ModelTypes.GroupByReturn<I, IO, R, RO, O>> = createOperation(
  $adapter,
  $shape,
  parseGroupByStatement,
  "rows",
);
