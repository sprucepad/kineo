/* prettier-ignore */
/* eslint-disable */
// This file is automatically generated. Any manual editions will be replaced.

// @ts-expect-error {{REMOVE_ON_GENERATE}}
import { adapter } from "./adapter.js";
import { createExec, createClose, createTransaction } from "kineo/runtime";

export { adapter as $adapter };

export const $transaction = createTransaction(adapter);
export const $exec = createExec(adapter);
export const $close = createClose(adapter);

`{{models}}`;
