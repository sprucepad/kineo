---
sidebar_position: 0
---

# Better-Auth

The [Better-Auth](https://www.better-auth.com/) integration gives you a schema function for Better-Auth and a Better-Auth adapter, so you can use Kineo as your database driver for Better-Auth.

## Installation

Install `@kineojs/better-auth` and Better-Auth:

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs>
  <TabItem value="npm">
```sh
npm install @kineojs/better-auth better-auth
```

  </TabItem>
  <TabItem value="yarn">
```sh
yarn add @kineojs/better-auth better-auth
```

  </TabItem>
  <TabItem value="pnpm">
```sh
pnpm add @kineojs/better-auth better-auth
```

  </TabItem>
  <TabItem value="bun">
```sh
bun add @kineojs/better-auth better-auth
```

  </TabItem>
</Tabs>

## Usage

1. Set the Kineo adapter as your Better-Auth database.

```ts
import { betterAuth } from "better-auth";
import { kineoAdapter } from "@kineojs/better-auth";
import { client } from "<your-database-client>";

export const auth = betterAuth({
  database: kineoAdapter(client),
  emailAndPassword: { enabled: true }, // example
});
```

2. Add the Better-Auth schema to your Kineo schema. Pass in all your Better-Auth plugins.

```ts
import { betterAuthSchema } from "@kineojs/better-auth";

export const schema = defineSchema({
  ...betterAuthSchema(),
});
```

That's it! You can now use Better-Auth with Kineo.
