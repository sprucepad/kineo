---
sidebar_position: 1
---

# Cypher emitter

The Cypher emitter, imported from `kineo/emitters/cypher`, only exports a single function as default: `emit`, which doesn't take a dialect; only the IR to compile.

See [the API reference](../../api/emitters/cypher) for more details.
